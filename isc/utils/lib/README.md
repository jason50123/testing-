# README

generated by compiling cpptrace source with:

```bash
cmake .. -DCMAKE_BUILD_TYPE=STATIC -DCPPTRACE_USE_EXTERNAL_ZSTD=true
```

in cmake, it should be linked with:

```cmake
target_link_libraries(${CMAKE_SOURCE_DIR}/lib/libcpptrace.a ${CMAKE_SOURCE_DIR}/lib/libdwarf.a z zstd dl)
```
